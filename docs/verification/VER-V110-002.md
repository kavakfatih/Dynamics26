# VER-V110-002 — Augmented-Lagrangian Contact State

## Amaç

Augmented-Lagrangian normal multiplier'ın committed/trial lifecycle ve committed-gap incremental update davranışını doğrulamak.

## Invariant

\[
\lambda_t=\max(0,\lambda_c-k_n(g_t-g_c))
\]

Aynı committed configuration tekrar evaluate edildiğinde `g_t=g_c` olduğundan multiplier/normal force değişmemelidir.

## Senaryo

1. İlk penetration multiplier üretir.
2. State commit edilir.
3. Aynı configuration yeniden evaluate edilir → multiplier sabit kalır.
4. İlave penetration → multiplier artar.
5. Revert → committed multiplier geri gelir.
6. Contact release → open state.

## Sonuç

`PASS VER-V110-002 augmented Lagrangian state`
