# VER-V100-003 — Mixed Newton Isochoric Simple Shear

Manufactured deformation:

```text
u_x = gamma y
gamma = 0.12
```

Bu deformation için `J=1`. V0.10 pressure constraint:

```text
p = K(J-1)
```

nedeniyle hedef pressure sıfırdır.

Hedef internal forces engine ile oluşturulur ve manufactured external load olarak uygulanır. Coupled Newton solver sıfır initial state'ten hedef simple-shear deformation'a yakınsar.

Kontroller:

- recovered `gamma = 0.12`
- final pressure residual ≈ 0
- element P0 pressure ≈ 0
- coupled displacement-pressure solve converged
