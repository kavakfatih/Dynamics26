# Dynamics26 V1.1.0-beta.3 — Implementation Status

**Milestone:** Supported Nonlinear Workflow Vertical Slice  
**Source baseline:** `2633cd36a435a2b591e14989b7d88c964c438d3b`  
**Branch:** `main`  
**USER VALIDATED:** ⬜

Bu belge Beta.3 implementation boyunca güncellenen teknik durum kaydıdır. Otomatik
test sonucu fiziksel macOS kullanıcı doğrulaması olarak işaretlenmez.

## Repository audit

- Güncel `main` HEAD başlangıç referansıyla eşleşti.
- Son tamamlanan milestone `V1.1.0-beta.2` ve aktif hedef Beta.3'tür.
- HEAD için hosted `macOS arm64 build and verification` run `33744743521`
  başarıyla tamamlandı.
- Current product mesh yalnız `StructuredHexMesher` + HEX8 box/boxDescriptor
  kapsamıdır.
- Current general `AnalysisService::solve()` yalnız `fem_solve_linear_hex8_mesh()`
  çağırır.
- Fortran `solve_nonlinear_static()` gerçek Newton/adaptive stepping/line-search
  altyapısını taşır; product-model C ABI bağlantısı eksiktir.
- Face → persistent Named Selection → BC/Load ObjectId zinciri mevcuttur ve
  canonical scope resolver'ı kullanır.

## Beta.3 gap matrix

| Work package | Repository baseline | Gap / karar | Durum |
|---|---|---|---|
| B3.0 Capability | Feature support kontrolleri `preflight()` içinde dağınık | Typed resolver; aynı resolution Preflight ve Solve tarafından tüketiliyor | Hosted CI passed |
| B3.1 Selection/scope | Face → Named Selection → Fixed Support/Force ve tek Undo macro mevcut | Canonical quick-authoring method + one/multi Face, Undo/Redo, stale/wrong-domain, save/reopen acceptance | In progress |
| B3.2 Total Force | `F_total / boundary_node_count` | QUAD4 2×2 Gauss consistent reference-surface integration | Pending |
| B3.3 Snapshot | `solve()` canlı service/model nesnelerinden array kuruyor | Immutable versioned `AnalysisSnapshot` + builder | Pending |
| B3.4 General ABI | Demo nonlinear ABI + general linear mesh ABI mevcut | Additive arbitrary HEX8 nonlinear v1 API | Pending |
| B3.5 Product bridge | NonlinearStatic Preflight tarafından bloklanıyor | Analysis type routing; nonlinear yolun DirectLinear'a düşmesi yasak | Pending |
| B3.6 Telemetry/results | Typed telemetry ve linear final fields mevcut | Real product Newton history + nonlinear displacement/stress/reaction | Pending |
| B3.7 Acceptance | Linear integrated workflow ve verification demos mevcut | Real document-model nonlinear vertical slice + persistence/equilibrium gates | Pending |

## B3.0 research / design record

### Problem

`AnalysisService::preflight()` engineering validation için doğru authoritative
boundary'dir; fakat analysis, material, formulation, kinematics, contact ve
backend support kararları doğrudan `if` bloklarına dağılmıştır. Bu durum Solve
içinde ikinci/gizli applicability kuralları oluşması riskini taşır.

### Adopt / adapt / reject

- **Adopt:** capability'yi typed axis + state ile çözmek.
- **Adapt:** ANSYS/COMSOL applicability yaklaşımını Dynamics26 document/service
  sınırlarına uyarlamak.
- **Keep:** `AnalysisService::preflight()` tek kullanıcı-facing engineering
  validation source olarak kalır.
- **Reject:** ikinci bağımsız validator ve Solve içinde capability tekrarları.

### Typed states

`Ready`, `SetupOnly`, `Unavailable`, `Stale`, `Invalid`.

### Evaluated axes

Analysis Type, Geometry Source, Mesh Topology, Element Formulation, Material
Model, Kinematics, Incompressibility/Formulation, Boundary Condition, Load Type,
Contact, Linear Backend, Nonlinear Algorithm ve Requested Result Field.

### Matrix metadata baseline

Displacement-only HEX8 için `symmetric / SPD expected`; mixed `u-p` için
`symmetric / indefinite`; active contact kombinasyonu için conservative
`unsymmetric / definiteness unknown` metadata üretilir. Current product backend
`dense reference` olarak doğru adla raporlanır; CG indefinite/unsymmetric sistemde
generic fallback değildir.

## B3.1 audit / design record

- `SelectionCoordinator` içindeki mevcut context-menu yolu doğru persistent
  relationship'i kuruyordu; ancak lambda doğrudan application acceptance altında
  değildi.
- Quick-authoring davranışı `createBoundaryConditionFromCurrentFaceSelection()`
  olarak tek canonical application method'una çıkarıldı. Context menu ve test aynı
  method'u tüketir.
- Method yalnız current, homogeneous `Geometry/Face` scope kabul eder. Mesh domain,
  yanlış kind ve stale CAD revision document mutation oluşturmadan reddedilir.
- Named Selection ve Fixed Support/Force ayrı domain command olarak korunur, fakat
  tek `QUndoStack` macro'sunda bir engineering transaction oluşturur.
- Pozitif CAD acceptance sahte topology ID üretmez. OCCT topology workflow'unun
  oluşturduğu gerçek STEP kutusunda one/multi Face, Undo/Redo ve project
  save/reopen ObjectId bütünlüğünü doğrular.

## Validation ledger

| Gate | CODE EXISTS | TEST EXISTS | TEST PASSED | FEATURE WORKS | USER VALIDATED |
|---|---:|---:|---:|---:|---:|
| B3.0 Capability resolver | ✅ | ✅ | ✅ hosted | ✅ hosted | ⬜ |
| B3.1 Selection/scope | ✅ | ✅ | ⬜ | ⬜ | ⬜ |
| B3.2 Surface force | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| B3.3 Snapshot | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| B3.4 General nonlinear ABI | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
| B3.5–B3.7 vertical slice | ⬜ | ⬜ | ⬜ | ⬜ | ⬜ |
