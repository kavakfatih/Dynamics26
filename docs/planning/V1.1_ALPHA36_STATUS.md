# Dynamics26 V1.1.0-alpha.3.6 — Implementation Status

## Scope

Alpha.3.6, Named Selection'ı saved raw ID listesi olarak değil, model graph üzerinde yaşayan reusable persistent engineering scope olarak ele alır.

## Source state

Source GUI milestone, functional + native visual acceptance tamamlandıktan sonra `1.1.0-alpha.3.6` değerine yükseltilmiştir. Fiziksel mouse/trackpad kabulü ayrı `USER VALIDATED` katmanı olarak izlenmeye devam eder.

### Uygulanan davranışlar

- `NamedSelectionService` persistent `ScopeReference` CRUD ve exact 64-bit persistence.
- Geometry scope için runtime revision + stable CAD engineering identity doğrulaması.
- Mesh scope için generation stale guard; numeric ID yeniden görünse bile silent rebind yok.
- Mixed domain ve mixed entity-kind scope reddi.
- `Edit Selection → Apply Selection / Cancel` transaction workflow'u.
- Valid stored scope edit başında güvenli transient preload; stale scope için sıfır old-ID preload.
- Normal Named Selection görünümünde stored scope'tan dinamik Geometry/Mesh context ve persistent overlay.
- Normal persistent overlay `SelectionManager` transient state'i değildir.
- Fixed Support / Force consumer'larında `NamedSelection ObjectId` reference; entity ID copy yok.
- Consumer preflight/dependency/solver resolution aynı authoritative scope resolver zincirini kullanır.
- Multi-face boundary presentation persistent resolver sonucu üzerinden çizilir; legacy tek-`BoxFace` fallback yoktur.
- Programatik `selectObject()` akışı Navigator `currentChanged → objectSelected` zincirini tek source of truth olarak kullanır; persistent Named Selection context'i ikinci shell dispatch ile ezilmez.

## Acceptance contract

```text
Persistent Named Selection
        ↓ validate current provenance
Valid? ──────────────── No
  │                    │
  │                    └─ OutOfDate/Error; old IDs not displayed
  ↓
Normal View: persistent overlay only
  │
Edit Selection
  ↓
Transient SelectionManager state
  ↓
Apply = one Undo command
Cancel = no document mutation
```

## Otomatik doğrulama durumu

### Functional application acceptance — PASS

GitHub-hosted Apple Silicon (`macos-15-arm64`) üzerinde gerçek Dynamics26 application composition ile:

- Selection shell acceptance: **74 / 74 PASS**.
- Boundary consumer acceptance: **34 / 34 PASS**.
- Native application gate: **Mach-O 64-bit executable arm64 PASS**.

Selection acceptance özellikle şunları doğrular:

- normal persistent FEM Named Selection stored scope'tan Mesh/Node context'ini çözer,
- persistent overlay transient `SelectionManager` state'i değildir,
- `Edit Selection` valid scope'u güvenli biçimde preload eder,
- `Apply Selection` tam bir document Undo transaction üretir,
- Undo/Redo eski/yeni persistent scope identity'yi geri getirir,
- `Cancel` kalıcı document mutation üretmez,
- mesh generation değişince scope `OutOfDate` olur,
- stale scope eski numeric Mesh ID'lerini preload etmez veya seçili göstermez,
- stale repair workflow doğru Named Selection project-object context'ini korur.

Boundary consumer acceptance özellikle şunları doğrular:

- Fixed Support ve Force `NamedSelection ObjectId` referansını exact 64-bit olarak persist eder,
- wrong-domain ve dangling referanslar legacy geometry fallback'e çevrilmez,
- stale scope consumer'a `OutOfDate`, dangling scope `Error` olarak yayılır,
- preflight invalid persistent scope'u solver öncesinde reddeder,
- mevcut legacy Geometry Selection solver yolu regresyona uğramaz.

### Clean macOS build / regression — PASS

Alpha.3.6 kaynak ağacı GitHub-hosted Apple Silicon üzerinde:

- Debug build + CTest: **PASS**,
- Release build + CTest: **PASS**,
- modal / nonlinear / hyperelastic / contact / OCCT / STEP / meshing gate'leri: **PASS**,
- install-layout ve installed C/C++ consumer smoke testleri: **PASS**,
- Qt 6 + VTK + OCCT `femcae_gui` clean build ve arm64 architecture gate: **PASS**.

### Native Light / Dark visual audit — PASS

Trusted macOS / Apple Silicon runner üzerinde Alpha.3.6 visual audit başarıyla tamamlanmıştır.

- Light appearance: **16 / 16 PNG PASS**.
- Dark appearance: **16 / 16 PNG PASS**.
- Normal Named Selection persistent görünümü: **PASS**.
- Valid `Edit Selection` preload görünümü: **PASS**.
- Stale Named Selection görünümü: **PASS**; eski Mesh ID marker'ı gösterilmez.
- Stale repair edit oturumu: **PASS**; Apply/Cancel ve reselect diagnostic görünürdür.

İlk visual audit denemesi trusted runner configure aşamasında log artefaktı üretmeden sonlandı; aynı source için failed job yeniden çalıştırıldığında configure, build, functional self-test, Light/Dark capture, screenshot validation ve artefakt upload adımlarının tamamı **SUCCESS** oldu. Bu nedenle doğrulanmış sonuç başarılı rerun'dır; ilk deneme ürün/source failure olarak sınıflandırılmaz.

Başarılı audit run'ı: `33514891326`. Screenshot artefaktı: `Dynamics26-ui-audit-93bb622b82c1426566d73dda4b7d7aa15c7f0211`.

Milestone define `1.1.0-alpha.3.6` olarak yükseltildikten sonra aynı visual/selection/topology gate'leri yeni milestone commit'i üzerinde yeniden tetiklenmiştir; kapanış kaydı bu final verification sonuçlarıyla birlikte tutulur.

### Physical interaction acceptance — PENDING

Mouse/trackpad Click, Shift+Click, Command+Click, window selection, Named Selection edit/apply/cancel ve görünürlük hissi otomatik CI ile `USER VALIDATED` sayılmaz. Fiziksel kullanıcı kabulü ayrı tutulur.

Tekrarlanabilir kullanıcı kabul adımları `docs/validation/V1.1_ALPHA36_MANUAL_ACCEPTANCE.md` içindedir.

## Durum terminolojisi

```text
CODE EXISTS      ✅
TEST EXISTS      ✅
TEST PASSED      ✅
VISUAL TEST      ✅
FEATURE WORKS    ✅
USER VALIDATED   ❌  (physical mouse/trackpad acceptance henüz yapılmadı)
```

Alpha.3.6 automated/native engineering acceptance kapanmıştır. Bundan sonraki GUI planı `V1.1.0-alpha.4 — Integrated Modeling Workflow + Preflight` aşamasına ilerler; fiziksel kullanıcı acceptance kaydı Alpha.3.6 manual protocol üzerinden ayrıca tamamlanabilir.
